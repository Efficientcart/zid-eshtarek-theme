{#
  Cart page — disabled for subscription-only theme.
  Zid may still route to this URL, so we render a redirect/message
  rather than a broken page.
#}
{% extends 'layout.jinja' %}

{% block content %}
<div class="container mx-auto px-4 py-16 text-center">
  <div class="max-w-md mx-auto">
    <svg class="mx-auto mb-6 w-16 h-16 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
    </svg>
    <h1 class="text-2xl font-bold mb-4">{{ _('This store uses subscriptions') }}</h1>
    <p class="text-gray-500 mb-8">{{ _('Browse our products and subscribe directly from the product page.') }}</p>
    <a href="/" class="inline-block bg-primary text-white px-8 py-3 rounded-lg font-medium hover:opacity-90 transition-opacity">
      {{ _('Browse Products') }}
    </a>
  </div>
</div>
{% endblock %}
