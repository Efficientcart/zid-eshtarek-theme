{#
  Shipping & Payment page — disabled for subscription-only theme.
  Eshtarek handles all payment and delivery configuration.
#}
{% extends 'layout.jinja' %}

{% block content %}
<div class="container mx-auto px-4 py-16 text-center">
  <div class="max-w-md mx-auto">
    <h1 class="text-2xl font-bold mb-4">{{ _('Shipping & Payment') }}</h1>
    <p class="text-gray-500 mb-8">{{ _('Delivery and payment are managed through your subscription. Visit your account to manage delivery preferences.') }}</p>
    <a href="/" class="inline-block bg-primary text-white px-8 py-3 rounded-lg font-medium hover:opacity-90 transition-opacity">
      {{ _('Browse Products') }}
    </a>
  </div>
</div>
{% endblock %}
